import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    mediaBox: {
        height: 200,
    },
    mediaImage: {
        maxWidth: '100%',
        maxHeight: '100%',
        margin: 'auto',
        width: 'auto',
    },
    mediaTitle: {
        alignSelf: 'center'
    }
}))
  